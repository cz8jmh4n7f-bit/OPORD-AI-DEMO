---
# Phase 2, step 1: prepare every node (OS prereqs, container runtime, k8s packages).
- name: Prepare all Kubernetes nodes
  hosts: k8s_cluster
  become: true
  gather_facts: true
  any_errors_fatal: true

  roles:
    - role: common
    - role: containerd
    - role: kubernetes_prereqs
